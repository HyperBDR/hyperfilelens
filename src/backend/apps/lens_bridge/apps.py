from django.apps import AppConfig


class LensBridgeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.lens_bridge"
    label = "lens_bridge"
    verbose_name = "Lens Bridge"
