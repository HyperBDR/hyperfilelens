"""Configuration center constants."""


CACHE_VERSION_KEY = "app_config:cache_version"
CACHE_KEY_PREFIX = "app_config"

# Sentinel: resolver did not find an active row (distinct from a stored null).
NOT_FOUND = object()
