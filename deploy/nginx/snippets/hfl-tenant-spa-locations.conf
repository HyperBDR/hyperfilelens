# Tenant-facing static application routes served by the active Web color pool.
    location / {
        proxy_pass http://hfl_web_tenant;
        include /etc/nginx/snippets/hfl-backend-proxy-headers.inc;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }
