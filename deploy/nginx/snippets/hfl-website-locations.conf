# Product website served by the active Web color pool.
    location / {
        proxy_pass http://hfl_website;
        include /etc/nginx/snippets/hfl-backend-proxy-headers.inc;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }
